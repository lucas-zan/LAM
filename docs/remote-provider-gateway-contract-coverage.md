# Remote Provider Gateway Contract Coverage

> Generated by `pnpm test:gateway-phase0 -- --write-report`. Do not edit by hand.

## Approved target

- Codex: exact-tested `0.144.1`
- Platform: `darwin arm64`, OS `15.6`
- Capture date: `2026-07-10`
- State mode: `full-input`; response store disabled; `previous_response_id` rejected

## Route contract

- Required and observed: `GET /v1/models`, `POST /v1/responses`
- Explicitly unsupported: `GET /v1/responses/{id}`, `POST /v1/responses/{id}`, `POST /v1/responses/{id}/cancel`

## Captured behavior

- **`text-stream`**: Codex sent stream=true to POST /v1/responses and accepted the normalized semantic SSE lifecycle through response.completed.
- **`function-tool`**: The follow-up request replayed full input including function_call and function_call_output with previous_response_id=null.
- **`resume`**: A persisted temporary session resumed by replaying full first-turn user and assistant items plus the new user item; previous_response_id remained null.
- **`route-inventory`**: Normal and failure cases observed only GET /v1/models and POST /v1/responses; retrieve and cancel routes were not called.
- **`auth-helper-trim`**: The helper received zero stdin bytes; surrounding whitespace was trimmed and requests carried the expected bearer value before redaction.
- **`auth-helper-empty`**: Codex reported an empty-token error but still sent three unauthenticated requests and completed because the capture server accepted them.
- **`auth-refresh-401`**: A first 401 caused one helper refresh and one replay; the second Responses request completed successfully.
- **`retry-429`**: A 429 with Retry-After 0 produced one Responses attempt and a terminal Codex error.
- **`retry-500`**: Default request and stream retry layers produced 30 Responses attempts before a terminal Codex error.
- **`malformed-sse`**: A malformed SSE payload produced six Responses attempts before a terminal Codex error.
- **`disconnect`**: Killing the isolated Codex process group after response.created closed the client connection observed by the server.

## Explicitly unsupported or unobserved

- **`text-non-stream`**: Every text, tool, and resume request emitted by codex exec 0.144.1 used stream=true; no non-stream CLI path was observed.

## Approved implementation decisions

- Store: `versioned-file-cross-process-lock-atomic-rename`; commit point `binding-cas-after-config-commit`.
- Credentials: storage and transport are separate; Gateway bearer is mandatory; token storage is `macos-keychain-only`.
- Retry: Codex request/stream retries `0/0`; Gateway application retries `0`; at most 2 connection attempts only before proven delivery.
- Sidecar identity: `signed-component-plus-same-uid-unix-peer-plus-nonce-hmac-plus-http-instance`.
- MVP platform policy: `exact-tested-macos-only`; unsupported platforms are read-only with zero Remote Provider mutation.
- Threat matrix IDs: `port_spoofing`, `token_exposure`, `auth_command_execution`, `ssrf_dns`, `sensitive_headers`, `resource_exhaustion`, `path_symlink`, `binding_rotation`, `content_leakage`, `version_mismatch`, `unsupported_platform`.

## Verification

The offline gate verifies manifest schema, exact target, mandatory cases and routes, two-run determinism, artifact ownership, byte counts, FNV-1a checksums, sanitization markers, approval consistency, and this report's exact generated content. It does not invoke Codex or network.
